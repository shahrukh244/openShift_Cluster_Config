# Use public NTP servers
pool 2.rhel.pool.ntp.org iburst

# Allow internal network to query time
allow 10.0.0.0/24

# Serve time even if upstream is temporarily unavailable
local stratum 10

# Drift file
driftfile /var/lib/chrony/drift

# Logging
logdir /var/log/chrony
